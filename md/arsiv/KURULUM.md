# Kurulum

ZIP içeriğini mevcut ana proje klasörünün üzerine kopyalayın.

```powershell
Ctrl + C
npm run check
npm run dev
```

Firestore kuralları bu sürümde değişmediği için `npm run firebase:deploy` gerekli değildir.
