{
  "name": "kuzenler-yarisiyor",
  "version": "5.6.1",
  "private": true,
  "type": "module",
  "description": "Premium mobil yönetim, korunan öğrenci işlemleri ve otomatik regresyon denetimi.",
  "scripts": {
    "prepare": "node scripts/prepare.mjs",
    "dev": "npm run prepare && node server.mjs",
    "start": "npm run dev",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run prepare && npm run test && node scripts/check-project.mjs && npm run build",
    "git:publish": "node scripts/git-publish.mjs",
    "firebase:deploy": "node scripts/deploy-firebase.mjs",
    "vercel:deploy": "node scripts/deploy-vercel.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
