import { MIDDLE_SCHOOL_TYMM_PHYSICAL_EDUCATION_ENGINE_RECORDS } from './middle-school-tymm-physical-education-engines.js';
const freeze=value=>{if(Array.isArray(value))return Object.freeze(value.map(freeze));if(value&&typeof value==='object')return Object.freeze(Object.fromEntries(Object.entries(value).map(([k,v])=>[k,freeze(v)])));return value;};
function pick(items){const first=items[0];const safety=items.find(item=>/güvenli ortam|ilk yardım/i.test(item.content.officialOutcomeText));const tactical=items.find(item=>/taktik ve strateji|liderliği/i.test(item.content.officialOutcomeText));return [first,safety||items[1],tactical||items.at(-1)];}
export function buildMiddleSchoolTymmPhysicalEducationReviewSample(){const rows=[];for(const record of MIDDLE_SCHOOL_TYMM_PHYSICAL_EDUCATION_ENGINE_RECORDS){for(const item of pick(record.items))rows.push({questionId:item.id,engineKey:`g${record.grade}:beden-egitimi-ve-spor`,grade:record.grade,courseId:record.courseId,outcomeIds:[...item.curriculum.outcomeIds],itemFormat:item.itemFormat,context:item.content.context,stem:item.content.stem,rubricCriteria:[...(item.responseModel.rubricCriteria||[])],hints:item.hints.map(h=>h.text),misconceptionIds:[...item.misconceptionIds],media:item.content.media,humanReviewStatus:'NOT_MEASURED',safeSpaceAndEquipmentStatus:'REQUIRED_NOT_ATTACHED',physicalEducationTeacherCalibration:'NOT_COMPLETED',gameAdaptationAllowed:false});}return freeze({schemaVersion:'1.0',generatedAt:new Date().toISOString(),status:'HUMAN_PHYSICAL_EDUCATION_TEACHER_AND_SAFETY_REVIEW_REQUIRED',metrics:{engineCount:3,itemCount:rows.length,perEngine:3,approved:0,pending:rows.length},rows});}
export function auditMiddleSchoolTymmPhysicalEducationReviewSample(sample=buildMiddleSchoolTymmPhysicalEducationReviewSample()){const errors=[];if(sample.metrics.engineCount!==3)errors.push('engine-count');if(sample.metrics.itemCount!==9)errors.push('item-count');if(sample.metrics.approved!==0||sample.metrics.pending!==9)errors.push('review-leak');if(new Set(sample.rows.map(r=>r.questionId)).size!==9)errors.push('duplicate');if(sample.rows.some(r=>r.gameAdaptationAllowed!==false||r.humanReviewStatus!=='NOT_MEASURED'||r.safeSpaceAndEquipmentStatus!=='REQUIRED_NOT_ATTACHED'||r.physicalEducationTeacherCalibration!=='NOT_COMPLETED'))errors.push('approval-safety-or-game-leak');return freeze({ok:errors.length===0,errors,metrics:sample.metrics});}
export const MIDDLE_SCHOOL_TYMM_PHYSICAL_EDUCATION_REVIEW_SAMPLE=buildMiddleSchoolTymmPhysicalEducationReviewSample();
export const MIDDLE_SCHOOL_TYMM_PHYSICAL_EDUCATION_REVIEW_SAMPLE_AUDIT=auditMiddleSchoolTymmPhysicalEducationReviewSample(MIDDLE_SCHOOL_TYMM_PHYSICAL_EDUCATION_REVIEW_SAMPLE);
